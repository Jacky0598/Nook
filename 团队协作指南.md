# 团队协作指南

给新加入的队友：读完这份就能开始干活。

---

## 一、两种"分享"，别搞混

| 你想干什么 | 用什么 | 给谁 |
|---|---|---|
| 让人**看看原型长什么样** | Netlify 链接 | 学生、潜在用户、投资人、朋友 |
| 让人**一起改代码** | GitHub 仓库 | 团队成员 |

绝大多数情况下你要的是第一种。**做用户调研不需要给学生 GitHub 链接**，他们既看不懂也不会去看。

---

## 二、分享原型给别人看

直接发这个链接：

**https://resonant-elf-0d405f.netlify.app**

- 手机打开自动全屏，体验接近真实小程序
- 不用下载任何东西，微信里直接点开就能用
- 可以随便发群里

### 改了原型之后怎么更新线上版本

1. 改 `web-preview/index.html`
2. 打开 https://app.netlify.com ，进入你的项目
3. 页面下方有个 "Drag and drop your project folder here to deploy new changes"
4. 把 `web-preview` **整个文件夹**拖进去
5. 十几秒后线上就更新了，**链接不变**

> 注意：拖的是**文件夹**，不是里面的 `index.html`。

---

## 三、拉队友进来改代码

### 第一步：仓库主人（Jacky）操作一次

1. 打开 https://github.com/Jacky0598/Nook
2. **Settings → 左边 Collaborators → Add people**
3. 输入队友的 GitHub 用户名，发出邀请

### 第二步：队友操作

1. 邮箱收到邀请，点接受
2. 没有 GitHub 账号先去 https://github.com 注册
3. 装 **GitHub Desktop**（https://desktop.github.com）并登录
4. 点 **Clone a Repository** → 选 `Jacky0598/Nook` → 选一个本地位置（比如桌面）→ Clone
5. 代码就下载到本地了

### 第三步：队友怎么跑起来

- **改小程序代码** → 微信开发者工具 → 导入项目 → 选 `Nook/miniprogram` 目录 → AppID 选测试号或游客模式
- **改原型页面** → 直接用浏览器打开 `web-preview/index.html`
- **改文档** → 任何编辑器打开 `.md` 文件

---

## 四、日常协作流程（每天都这么走）

```
① 开工前：Pull（拉取）
      ↓
② 改代码
      ↓
③ Commit（提交）+ 写清楚改了什么
      ↓
④ Push（推送）
```

### 在 GitHub Desktop 里

1. **Fetch origin**（右上角）→ 如果有更新会变成 **Pull origin**，点它
2. 改完代码，左边自动列出改动的文件
3. 左下角输入框写说明，点 **Commit to main**
4. 点右上角 **Push origin**

### 在 VS Code 里

1. 左边第三个图标（分叉状）= **源代码管理**
2. 点顶部 **⋯ → 拉取（Pull）**
3. 改完后，改动文件旁点 **+** 暂存
4. 上面输入框写说明 → 点 **✓ 提交**
5. 点 **同步更改（Sync Changes）**

两个工具操作的是同一个仓库，可以混着用。

---

## 五、三条铁律

### ① 开工前必须先 Pull

不拉最新代码就开始改，是**所有冲突的根源**。养成习惯：坐下来第一件事就是 Pull。

### ② 别两个人同时改同一个文件

提前分工。比如：

| 人 | 负责 |
|---|---|
| A | `pages/index/`、`pages/explore/` |
| B | `pages/publish/`、`pages/detail/` |
| C | `utils/mock.js`、文档 |

要改别人的文件，先在群里说一声。

### ③ Commit 说明写人话

❌ `test`、`update`、`111`、`修改`
✅ `修复比价页选中状态不刷新`
✅ `新增留学咨询类目筛选`
✅ `PRD 补充房产线合规方案`

三个月后你会感谢自己。

---

## 六、冲突了怎么办

如果两个人改了同一个文件的同一处，Push 时会报冲突。**别慌，不会丢代码。**

1. VS Code 会把冲突的文件标红，打开后长这样：

```
<<<<<<< HEAD
你的版本
=======
队友的版本
>>>>>>> origin/main
```

2. VS Code 会在上方给你四个按钮：**采用当前更改 / 采用传入更改 / 保留双方更改 / 比较**
3. 选一个，或者手动编辑成你想要的样子，把 `<<<<<<<`、`=======`、`>>>>>>>` 这些标记删干净
4. 保存 → 暂存 → 提交 → 推送

**实在解不开**：把你改的内容复制出来存到别处，然后
```bash
git reset --hard origin/main
```
（这会丢弃你所有未推送的本地改动，回到跟 GitHub 一致的状态），再把改动手动贴回去。

---

## 七、绝对不要做的事

- ❌ **不要把密码、API 密钥、数据库连接串写进代码。** 仓库是 **public**，全世界可见，有爬虫专门扫 GitHub 找泄露的密钥。要用就放 `.env` 文件（已在 `.gitignore` 里挡掉）
- ❌ **不要 `git push --force`。** 会覆盖掉队友的提交，且难以恢复
- ❌ **不要删 `.gitignore`**
- ❌ **不要把 `node_modules/` 提交上去**

---

## 八、以后团队变大了

现在三五个人，全在 `main` 分支上干活是最简单的。等人多了或者开始上线，再引入分支流程：

```
每个功能开一个分支 → 做完发 Pull Request → 队友 Review → 合并进 main
```

好处是 `main` 永远是能跑的版本，坏处是多几步操作。**现阶段不需要，别过早增加复杂度。**

---

## 九、常见问题

**Q：我改的东西队友看不到？**
你 Commit 了但没 Push。Commit 只是存到本地，Push 才上传。

**Q：`cannot lock ref` / `index.lock exists`？**
残留的锁文件。确认没有 git 进程在跑，然后：
```bash
rm -f .git/*.lock .git/refs/heads/*.lock
```

**Q：不小心删了文件怎么恢复？**
只要之前提交过，就在历史里。
```bash
git restore 文件名
```
或在 VS Code 源代码管理面板里点该文件旁的**撤销**箭头。

**Q：Netlify 链接名字太丑？**
Netlify 项目页 → Domain management → Options → Edit site name，可以改成 `campus-my` 之类的。
